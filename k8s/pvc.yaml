apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: infra-bot-data
  namespace: infra-bot
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""  # Use static PV instead of dynamic provisioning
  volumeName: infra-bot-pv
  resources:
    requests:
      storage: 1Gi
